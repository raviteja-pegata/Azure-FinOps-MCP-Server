"""Tests for Azure FinOps MCP tools.

To run: pytest tests/ (install pytest first: pip install pytest)

For now, the fastest way to test is the MCP Inspector:
    npx @modelcontextprotocol/inspector $(which python3) -m azure_finops_mcp.server
"""
